// Diff viewer panel (EKI-105, M3 T16) — registry stub until the viewer lands.
import { EmptyState } from "@/components/EmptyState";

export default function DiffPanel() {
  return (
    <EmptyState emoji="🧘" title="Working tree is clean" hint="The diff viewer is on its way (EKI-105)" />
  );
}
