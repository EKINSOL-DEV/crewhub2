{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-window",
  "description": "Least-privilege capability for the dedicated settings window",
  "windows": ["settings"],
  "permissions": ["core:default"]
}
